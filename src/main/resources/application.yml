spring:
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
rate-limiting:
  capacity: ${RATE_LIMIT_CAPACITY:10}
  refill-rate: ${RATE_LIMIT_REFILL_RATE:1}

# Redis connection logger active
